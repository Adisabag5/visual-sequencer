/** Development. Swapped for environment.production.ts by an angular.json fileReplacement. */
export const environment = {
  production: false,
  /** Must match PORT in nest-server/.env. */
  apiBaseUrl: 'http://localhost:3000',
};
